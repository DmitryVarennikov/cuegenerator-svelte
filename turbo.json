{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".svelte-kit/**", ".vercel/**"]
    },
    "lint": {},
    "test": {},
    "pre-deploy": {
      "dependsOn": ["lint", "test", "build"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
